# سیستم چت پشتیبانی شهرزاد

وب اپلیکیشن چت پشتیبانی با طراحی مدرن و mobile-first که به API چت خارجی متصل است.

## ویژگی‌ها

- 📱 طراحی Mobile-First و Responsive
- 💬 چت آنلاین با ربات هوشمند
- 🎨 رنگ‌بندی زیبا با تم آبی
- 📂 مدیریت جلسات چت متعدد
- 🔄 ذخیره خودکار تاریخچه گفتگوها
- 🌐 اتصال به API خارجی برای دریافت پاسخ‌ها
- 🎯 سایدبار قابل حذف برای موبایل

## پیش‌نیازها

- Node.js 18 یا بالاتر
- npm یا yarn
- حساب Supabase

## نصب و راه‌اندازی

### 1. نصب وابستگی‌ها

```bash
npm install
```

### 2. تنظیمات محیطی

فایل `.env` را با اطلاعات خود پر کنید:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_CHAT_API_URL=http://103.75.196.71:8020/chat
```

### 3. اجرای محلی

```bash
npm run dev
```

اپلیکیشن در `http://localhost:5173` در دسترس خواهد بود.

### 4. ساخت برای Production

```bash
npm run build
```

## استقرار با Docker

### پیش‌نیازها

مطمئن شوید که API Backend شما روی پورت 8020 در حال اجرا است:
```bash
# بررسی وضعیت API
curl http://172.17.0.1:8020/chat
```

### ساخت و اجرای کانتینر

```bash
# توقف کانتینر قبلی (اگر وجود دارد)
docker-compose down

# ساخت مجدد ایمیج
docker-compose build --no-cache

# اجرای کانتینر
docker-compose up -d

# مشاهده لاگ‌ها
docker-compose logs -f
```

### دسترسی به اپلیکیشن

پس از استقرار، اپلیکیشن در آدرس زیر در دسترس است:

```
http://103.75.196.71:8082
```

### عیب‌یابی

اگر مشکل CORS دارید:

1. بررسی کنید Nginx به درستی راه‌اندازی شده است:
```bash
docker exec -it <container-name> nginx -t
```

2. بررسی لاگ‌های Nginx:
```bash
docker logs <container-name>
```

3. تست proxy:
```bash
curl http://103.75.196.71:8082/chat -X POST \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test","message":"سلام"}'
```

## ساختار پروژه

```
src/
├── components/
│   ├── WelcomePage.tsx      # صفحه خوشامدگویی
│   ├── ChatInterface.tsx    # محیط چت
│   └── Sidebar.tsx          # سایدبار مدیریت جلسات
├── lib/
│   └── supabase.ts          # کلاینت Supabase
├── App.tsx                  # کامپوننت اصلی
└── main.tsx                 # نقطه ورود
```

## API چت

اپلیکیشن به API خارجی متصل است:

**Endpoint Backend:** `http://172.17.0.1:8020/chat`

**Method:** `POST`

### حل مشکل CORS

برای جلوگیری از خطای CORS، از Nginx Proxy استفاده شده است:

- **Frontend Request:** `/chat` (relative path)
- **Nginx Proxy:** `http://172.17.0.1:8020/chat`

این روش باعث می‌شود درخواست‌ها از همان domain ارسال شوند و مشکل CORS حل شود.

**Request Body:**
```json
{
  "session_id": "string",
  "message": "string"
}
```

**Response:**
```json
{
  "response": "string"
}
```

## دیتابیس

### جداول

- `chat_sessions`: ذخیره جلسات چت
- `messages`: ذخیره پیام‌های کاربر و ربات

### RLS (Row Level Security)

- سیاست‌های امنیتی برای دسترسی عمومی (برای دمو)
- در محیط واقعی باید احراز هویت اضافه شود

## استفاده از اپلیکیشن

### موبایل

1. صفحه خوشامدگویی با عکس تهران نمایش داده می‌شود
2. روی دکمه "شروع گفتگو" کلیک کنید
3. با کلیک روی آیکون منو (☰) سایدبار باز می‌شود
4. پیام‌های کاربر در سمت راست و پاسخ ربات در سمت چپ نمایش داده می‌شود

### دسکتاپ

1. سایدبار همیشه نمایش داده می‌شود
2. با کلیک روی لوگو در سایدبار به صفحه اصلی برمی‌گردید
3. می‌توانید چند چت همزمان داشته باشید

## دستورات مفید

```bash
# اجرای محیط توسعه
npm run dev

# ساخت برای production
npm run build

# پیش‌نمایش build
npm run preview

# بررسی کد
npm run lint

# بررسی تایپ‌ها
npm run typecheck
```

## تکنولوژی‌ها

- React 18
- TypeScript
- Vite
- Tailwind CSS
- Supabase
- Lucide Icons
- Docker & Nginx

## توسعه‌دهنده

این پروژه برای سیستم چت پشتیبانی شهرزاد توسعه داده شده است.

## مجوز

این پروژه تحت مجوز خصوصی است.
